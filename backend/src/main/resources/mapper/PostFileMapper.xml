<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.board.mapper.PostFileMapper">

    <insert id="save">
        INSERT INTO file (post_id, original_name, saved_name, file_path, file_size)
        VALUES (#{postId}, #{originalName}, #{savedName}, #{filePath}, #{fileSize})
    </insert>

    <!-- 게시글 조회시 파일 불러오기 -->
    <select id="findByPostId" resultType="PostFile">
        SELECT * FROM file WHERE post_id = #{postId}
    </select>
    
    <delete id="deleteById">
        DELETE FROM file WHERE file_id = #{fileId}
    </delete>

</mapper>
